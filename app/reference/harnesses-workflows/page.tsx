import Link from 'next/link';import ReferenceChrome from '../ReferenceChrome';import {pillars,sources} from '../referenceData';
const p=pillars[0];const patterns=[['Observable agent loop','Every step has an owner, state, and stop condition.'],['Context budget','Freshness, relevance, and disclosure limits stay visible.'],['Tool contract','Inputs, outputs, errors, authority, and effects are explicit.'],['Handoff packet','Goal, owner, state, evidence, restrictions, and acceptance travel together.'],['Resume after interruption','Read current state before repeating an external effect.'],['Ask for the missing decision','Research first; ask only where a person must choose.'],['Source-of-truth refresh','Recheck action-driving facts at the system that owns them.']];
export const metadata={title:'Harnesses & Workflows · Ayodhya reference'};
export default function Harness(){return <ReferenceChrome><article className="pillar"><header className="pillar-head"><div><p>01 · HARNESSES & WORKFLOWS <em>TESTED PRINCIPLES</em></p><h1>Design the environment<br/>around the model.</h1><span>The product is the loop: context, tools, handoffs, memory, checkpoints, and the interface that makes each step legible.</span></div><aside><b>Editorial definition</b><p>A harness is the operating environment that turns a model response into sustained work. It selects context, exposes tools, records state, routes handoffs, and decides where a person must intervene.</p></aside></header><section className="loop"><div>{['Intent','Plan','Context','Tool call','Observation','State update','Checkpoint','Continue / Ask / Stop'].map((x,i)=><span key={x}>{x}{i<7?<i>→</i>:null}</span>)}</div><p>HUMAN CONTROL · STEER · APPROVE · INSPECT · RECOVER</p></section><section className="pillar-copy"><aside><a href="#argument">Why it changes the product</a><a href="#topics">Topic index</a><a href="#patterns">Patterns</a><a href="#failures">Failure modes</a><a href="#sources">Sources</a></aside><div><section id="argument"><p className="kicker">WHY THIS CHANGES THE PRODUCT</p><h2>A polished chat surface cannot repair a weak loop.</h2><p>Context should be selected for the next decision, not accumulated without limit. Tool interfaces are product interfaces: names, parameters, responses, and errors shape agent behavior. Handoffs must carry the request, state, evidence, restrictions, and unresolved decision, not merely a summary.</p><p>Memory, progress, and permissions are separate system concerns. If the interface collapses them into a confident answer, it hides the product&apos;s real state.</p></section><section id="topics"><p className="kicker">FIVE TOPICS</p>{p.topics.map((t,i)=><div className="topic" key={t.title}><small>0{i+1}</small><h3>{t.title}</h3><p>{t.question}</p></div>)}</section><section id="patterns"><p className="kicker">PATTERNS TO USE</p><div className="pattern-list">{patterns.map(x=><div key={x[0]}><h3>{x[0]}</h3><p>{x[1]}</p><span>RELEASE CHECK</span></div>)}</div></section><blockquote>“Do not design the agent&apos;s answer. Design the conditions under which it can keep working without losing truth, control, or the next decision.”<cite>Ayodhya&apos;s take</cite></blockquote><section id="failures"><p className="kicker">FAILURE MODES</p><table><tbody>{[['More context, less relevance','Stale or contradictory action'],['Tool name hides effect','Wrong action or unsafe approval'],['Summary-only handoff','Lost restrictions and evidence'],['Progress inferred from narration','Claims work that did not land'],['Retry without idempotency','Duplicate external effect'],['Memory treated as authority','Habit becomes unauthorized action']].map(x=><tr key={x[0]}><th>{x[0]}</th><td>{x[1]}</td></tr>)}</tbody></table></section><section id="sources"><p className="kicker">PRIMARY SOURCES · REVIEWED 16 SEP 2026</p>{sources.map(s=><a className="source-row" key={s.url} href={s.url} target="_blank" rel="noopener noreferrer"><small>{s.publisher}</small><b>{s.title}</b><i>↗</i></a>)}</section></div></section></article></ReferenceChrome>}
