'use client';
import {useState} from 'react';
export default function CasePlay(){const [stage,setStage]=useState(0),[signal,setSignal]=useState(62),[approved,setApproved]=useState(false);return <section className="case-visual case-play"><p>PLAY WITH THE SYSTEM</p><div className="case-play-tabs">{['Observe','Frame','Decide'].map((x,i)=><button onClick={()=>setStage(i)} className={stage===i?'on':''} key={x}>{x}</button>)}</div><div className="case-play-state"><b>{['Collect the signal','Name the constraint','Choose the tradeoff'][stage]}</b><span>{['Scan evidence before proposing UI.','Keep uncertainty visible.','Show consequence before action.'][stage]}</span></div><label>Signal strength <b>{signal}%</b><input type="range" min="0" max="100" value={signal} onChange={e=>setSignal(+e.target.value)}/></label><button className={approved?'approved':''} onClick={()=>setApproved(!approved)}>{approved?'Decision approved ✓':'Review decision'}</button></section>}
