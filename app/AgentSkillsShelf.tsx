import {agentDesignSkills,agentSkillGroups,groupedAgentSkills} from './designShelvesData';
export default function AgentSkillsShelf(){return <section id="agent-design-skills" className="ay-section skill-shelf"><p className="ay-eye">AGENT DESIGN SKILLS · {agentDesignSkills.length} STUDIED</p><h2>Agent design skills.</h2><p className="ay-lede">The skills, guardrails, and workspaces I study to keep agent-made interfaces specific, usable, and true to their product.</p>{groupedAgentSkills().map(([group,items])=><div className="skill-group" key={group}><header><h3>{(agentSkillGroups[group]??agentSkillGroups.other).label}</h3><span>{items!.length}</span></header><div>{items!.map(item=><article key={item.id} id={`skill-${item.id}`}><p><span>{item.type}</span>{item.status&&<b>{item.status}</b>}</p><h4>{item.name}</h4><small>{item.publisher}{item.license&&` · ${item.license}`}</small><p>{item.whyItMatters}</p><em>{item.lesson}</em><ul>{item.capabilities.map(x=><li key={x}>{x}</li>)}</ul><a href={item.canonicalUrl} target="_blank" rel="noreferrer">Open source ↗</a>{item.id==='skills-designers-engineers'&&<a href="#person-emil-kowalski">Followed creator: Emil Kowalski ↓</a>}{item.relatedResources.length>0&&<details><summary>{item.relatedResources.length} related</summary>{item.relatedResources.map(r=><a key={r.url} href={r.url} target="_blank" rel="noreferrer">{r.label}</a>)}</details>}</article>)}</div></div>)}</section>}
