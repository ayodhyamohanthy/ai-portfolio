import Link from 'next/link';import TopBoards from './TopBoards';import {denseBoards} from '../benchData';import Reveal from '../Reveal';
import SystemTrail from '../SystemTrail';
export const metadata={title:'Benchmarks · Ayodhya Mohanthy',description:'The Design Arena leaderboard, mirrored live - every arena and cut, plus the full 211-model board.'};
export default function Benchmarks(){return <main className="resource-page atlas-page fresh-page tools-page benchmarks-page">
<nav className="atlas-nav"><Link href="/" className="wordmark">Ayodhya Mohanthy</Link><Link href="/system">System</Link><div><Link href="/components">Components</Link><Link href="/sources">Sources</Link><Link href="/atlas">Atlas</Link><Link href="/fresh">Fresh</Link><Link href="/tools">Tools</Link><Link href="/thinking">Thinking</Link></div></nav>
<header className="atlas-hero"><p className="eyebrow">BENCHMARKS · AI-NATIVE DESIGN · THE LIVE BOARD, MIRRORED</p><h1>The design leaderboard.<br/><i>Mirrored, and kept live.</i></h1><p>Two views of the same live board: the full Design Arena frame - every arena and cut, both metrics, all three model filters - and the complete 211-model mirror below it. Every number is read off the live board on the date stamped and refreshed hourly - follow the link and check it yourself.</p><p className="bench-toc"><a href="#top-boards">Top leaderboards ↓</a><a href="#mirror">Full mirror ↓</a></p></header><SystemTrail current="Evaluate"/>
<section className="sources-track" id="top-boards" data-rv>
<div className="track-head"><b>01</b><div><h2>Top leaderboards</h2><p>The Design Arena frame, mirrored: every arena and cut on the board - code, image, video, audio, slides, SVG and ASCII - with the Elo / win-rate toggle and the All Models / Open Weights / Routers filter, all working off live board data read 13 Sep 2026. Cuts with no public data feed link straight to the live board. Refreshes on the sweep.</p></div></div>
<TopBoards/>
</section>
<div id="mirror"/>{denseBoards.filter(d=>d.board==='Design Arena').map((d,di)=>{const top=(ci:number)=>new Set(d.rows.map(r=>parseFloat(r[ci])).sort((a,b)=>b-a).slice(0,5));const t1=top(2),t2=top(d.cols.length>4?4:3);return <section className="sources-track bench-snapshot" key={d.category} data-rv>
<div className="track-head"><b>{String(di+2).padStart(2,'0')}</b><div><h2>{d.board}</h2><p>{d.category} · {d.what}</p></div></div>
<table className="bench-table bench-dense lb-table"><thead><tr>{d.cols.map((c,ci)=><th key={c} className={ci>1?'num':''}>{c}</th>)}</tr></thead><tbody>{d.rows.map(r=><tr key={r[1]}>{r.map((cell,ci)=>{const v=parseFloat(cell);const shaded=(ci===2&&t1.has(v))||((ci===(d.cols.length>4?4:3))&&t2.has(v));return <td key={ci} className={`${ci===0?'bench-rank':ci>1?'num bench-score':''} ${ci===2?'lb-global':''} ${shaded?'lb-top':''}`}>{cell}</td>})}</tr>)}</tbody></table>
<p className="lb-foot">// elo · higher is better · shading = top 5 per column · mirrored from {d.url.replace('https://','')} · checked {d.checked.toLowerCase()}</p>
</section>})}
<footer className="resource-footer"><Link href="/">Ayodhya Mohanthy</Link><span>The frame + the full mirror · refreshed hourly · reviewed 13 Sep 2026</span><a href="https://x.com/ayodhyamohanthy" target="_blank" rel="noopener noreferrer">Follow on X ↗</a><a href="https://www.linkedin.com/in/ayodhyamohanthy/" target="_blank" rel="noopener noreferrer">LinkedIn ↗</a><a href="https://github.com/ayodhyamohanthy" target="_blank" rel="noopener noreferrer">GitHub ↗</a><a href="https://ayodhyamohanthy.substack.com" target="_blank" rel="noopener noreferrer">Newsletter ↗</a><Link href="/accessibility">Accessibility ↗</Link></footer><Reveal/></main>}
