import Link from 'next/link';import {capabilities} from '../capabilityData';
const statusClass=(s:string)=>s.startsWith('Strong')?'strong':s.startsWith('Partial')?'partial':'needs';
export default function Capabilities(){return <main><nav><Link href="/" className="wordmark">Ayodhya Mohanthy</Link><div><Link href="/">Portfolio</Link><Link href="/library">Starter kit</Link><Link href="/system">System</Link></div></nav><header className="cap-hero"><p className="eyebrow">12-STAGE EVIDENCE MAP</p><h1>What the portfolio proves. What it does not. Yet.</h1><p>A competency map tied to live evidence, not a wall of skill badges. Strong means you can inspect the work. Partial means the thinking is visible but the original artifact is not. Needs evidence means no claim.</p><div className="cap-legend"><span className="strong">5 strong</span><span className="partial">6 partial</span><span className="needs">1 needs evidence</span></div></header><section className="cap-list">{capabilities.map(x=><article className="cap-row" id={`stage-${x.n}`} key={x.n}><div className="cap-num">{String(x.n).padStart(2,'0')}</div><div className="cap-main"><div className={`cap-status ${statusClass(x.status)}`}>{x.status}</div><h2>{x.title}</h2><p>{x.summary}</p><div className="cap-proof">{x.proof.map(p=><a href={p.href} key={p.href}>{p.label} ↗</a>)}</div></div><div className="cap-missing"><b>To strengthen this</b>{x.missing.map(m=><span key={m}>+ {m}</span>)}</div></article>)}</section><section id="ia-flow" className="ia-proof"><div><p className="eyebrow">NATIVE IA PROOF</p><h2>The portfolio&apos;s own decision flow.</h2><p>One first-screen choice, then progressive disclosure. A recruiter can reach proof without using the adaptive layer; the intelligent features speed the scan instead of becoming a gate.</p></div><div className="ia-map"><span>Arrival signals</span><i>→</i><span>Choose intent</span><i>→</i><span>Proof overview</span><i>→</i><span>Case evidence</span><i>→</i><span>Contact</span></div><div className="ia-branches"><article><b>Review the work</b><p>Product depth first → five shipped-product cases → two AI-native explorations.</p></article><article><b>Use the starter kit</b><p>Search → filter → inspect source → copy an artifact.</p></article><article><b>Open the lab</b><p>Case framing → decisions → twenty live interface patterns.</p></article></div></section></main>}
