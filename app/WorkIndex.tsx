import Link from 'next/link';import {cases} from './data';
const productSlugs=['application-automation','clinical-trial-tool','data-product-design','telemetry','ad-platform'];
export default function WorkIndex(){const product=productSlugs.map(s=>cases.find(c=>c.slug===s)!),ai=['ai-interface-exploration','orbit'].map(s=>cases.find(c=>c.slug===s)!);const group=(label:string,title:string,list:typeof cases,start:number)=><div className="work-group"><div className="work-group-head"><p className="eyebrow">{label}</p><h3>{title}</h3><span>{list.length} projects</span></div><div className="case-list">{list.map((c,i)=><Link href={`/work/${c.slug}`} className={`case ${c.accent}`} key={c.slug}><div className="case-num">{String(start+i).padStart(2,'0')}</div><div><p className="eyebrow">{c.kicker.replace(/^\d+ \/ /,'')}</p><h3>{c.title}</h3><p>{c.summary}</p><div className="chips">{c.category.map(x=><span key={x}>{x}</span>)}</div></div><div className="impact">{c.impact.map(x=><span key={x}>{x}</span>)}<b>Read case ↗</b></div></Link>)}</div></div>;return <section id="work"><div className="section-head"><div><p className="eyebrow">THE WORK / 7 CASES</p><h2>Start with depth. Then inspect the AI-native practice.</h2></div><p>Five shipped-product case studies establish business, research and systems depth. Two current explorations show how Ayodhya designs for AI.</p></div>{group('01 / PRODUCT SYSTEMS','Complex products, made usable.',product,1)}{group('02 / AI-NATIVE PRACTICE','Interfaces and methods for uncertain systems.',ai,6)}</section>}
